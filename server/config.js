// Authoritative list of access tokens accepted by the backend.
// The client mirrors these in src/config.js only as UI hints; the server
// is the source of truth for what can actually read/write state.
export const ACCESS_TOKENS = ['growtheffekt-demo', 'leadhunt-2024', 'admin-preview'];
